let ZNFT = artifacts.require("./ZNFT.sol");
let zNFT;

contract("Test ZYJ NFT", (accounts) => {

    before(async () => {
        zNFT = await zNFT.deployed();
        // Have tested on remix by hand. Don't test here.
    });


})
